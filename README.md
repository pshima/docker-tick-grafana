# Influx 2 Docker

This repository will setup a host with docker and influxdb with telegraf for monitoring.

This uses only influx 2 syntax, config and binaries.

This is a counterpart to a raspberry pi version of this so it probably only works on Debian.